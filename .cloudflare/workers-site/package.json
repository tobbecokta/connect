{
  "private": true,
  "name": "sms-workers-site",
  "version": "1.0.0",
  "description": "SMS app workers site configuration",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
} 